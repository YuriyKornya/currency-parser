version: '3.8'

services:
  mysql:
    image: mysql:8.0
    container_name: mysql-parsing
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: parsingDB
    ports:
      - "3310:3306"
    volumes:
      - mysql_data:/var/lib/mysql

volumes:
  mysql_data: